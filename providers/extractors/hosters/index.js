'use strict';

const { extractLoadm, isLoadmUrl } = require('./loadm');
const { extractMixdrop, isMixdropUrl } = require('./mixdrop');

module.exports = {
    extractLoadm,
    isLoadmUrl,
    extractMixdrop,
    isMixdropUrl
};
